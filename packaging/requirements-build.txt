# Extra dependencies for building/running the packaged desktop app.
# Install alongside requirements.txt:  pip install -r requirements.txt -r packaging/requirements-build.txt
pyinstaller>=6.10
pystray>=0.19
